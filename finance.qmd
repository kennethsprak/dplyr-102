---
title: "finance"
format: html
editor: visual
---

```{r}
library(vroom)
library(ggplot2)
library(dplyr)
library(tidyr)
library(here)
```
```{r}

```


```{r}
clients <- vroom(here("data", "client.csv"))
accounts <- vroom(here("data", "account.csv"))
disp <- vroom(here("data", "disp.csv"))
```

